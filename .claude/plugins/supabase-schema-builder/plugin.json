{
  "name": "supabase-schema-builder",
  "version": "1.0.0",
  "description": "Interactive schema design wizard for Supabase with cross-platform model generation. Onboards new team members, designs entities through a guided wizard, and keeps SQL migrations, TypeScript types, Swift models, and Kotlin data classes in sync via MCP.",
  "skills": "skills/",
  "agents": "agents/",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/migration-model-sync-reminder.py"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/model-schema-sync-reminder.py"
          }
        ]
      }
    ]
  }
}
